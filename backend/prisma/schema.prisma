generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Roteiro {
  id        String   @id @default(uuid())
  nome      String
  data      String
  genero    String
  lugares   String?
  criadoEm  DateTime @default(now())
}
