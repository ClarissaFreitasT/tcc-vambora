datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Roteiro {
  id      Int     @id @default(autoincrement())
  nome    String
  data    String
  genero  String
  lugares String?
  createdAt DateTime @default(now())
}
